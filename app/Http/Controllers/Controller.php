<?php

namespace App\Http\Controllers;

/**
 * Contrôleur de base de l'application Tonji.
 *
 * Toutes les classes de contrôleurs héritent de cette classe abstraite.
 * Elle est intentionnellement vide : Laravel n'impose plus d'héritage
 * depuis la version 10 (plus de AuthorizesRequests, ValidatesRequests…).
 * Elle sert de point d'extension futur (helpers communs, middleware partagé).
 */
abstract class Controller
{
    //
}
